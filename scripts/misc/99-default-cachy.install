#!/bin/sh
# /etc/kernel/install.d/99-default-cachy.install
set -e

COMMAND="$1"

# Only run when a kernel is being installed/updated
if [ "$COMMAND" = "add" ]; then
    grubby --set-default=/boot/$(ls /boot | grep vmlinuz.*cachy | sort -V | tail -1)
fi
