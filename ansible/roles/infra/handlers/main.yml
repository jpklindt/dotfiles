#SPDX-License-Identifier: MIT-0
---
# handlers file for roles/infra
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
    scope: user

- name: Restart caddy service
  ansible.builtin.systemd:
    name: caddy.service
    state: restarted
    scope: user
