[defaults]
# Vagrant's ansible provisioner still passes the deprecated --inventory-file
# flag; this repo's own playbooks don't trigger deprecations.
deprecation_warnings = False
interpreter_python = auto_silent
